package Compressor;

import Layer3.IP;

public class Modes {
	static IP lastIp;

	public static String IP_Only(IP ip) {
		if(lastIp==null || lastIp!=ip) {
			return ip.toString();
		}
		
		return null;
		
	}
	
}
